language: node_js

node_js:
  - "10.7.0"

services:
  - postgresql

before_script:
  - psql -c 'create database mydiary;' -U postgres

cache:
  directories:
    - "node_modules"
    
after_success: npm run coverage